import React from 'react';
import classes from './settings.module.css';
const Settings = () => {
    return (
        <nav className={classes.settingsBar}>
            User's settings will be located here.
        </nav>
    );
}

export default Settings;